# Core ML
numpy>=1.24.0
transformers>=4.45.0
tokenizers>=0.19.0

# LoRA / preference fine-tuning
peft>=0.10.0
trl>=0.9.0          # DPOTrainer, ORPOTrainer
accelerate>=0.27.0

# First-stage retrieval
rank-bm25>=0.2.2    # simple in-memory BM25 (no Java required)
# pyserini>=0.24.0  # production BM25 (Pyserini, requires Java 11+)

# Dense retrieval / ANN index
faiss-cpu>=1.7.4    # swap to faiss-gpu if GPU available
sentence-transformers>=2.2.0

# Data processing
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# Serving (optional)
fastapi>=0.110.0
uvicorn>=0.29.0
